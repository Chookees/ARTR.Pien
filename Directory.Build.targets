<Project>
  <!-- Coverage thresholds enforced in CI via coverlet collector + future ReportGenerator gates. -->
  <PropertyGroup Condition="'$(IsTestProject)' == 'true'">
    <CollectCoverage>true</CollectCoverage>
  </PropertyGroup>
</Project>
